package promptgen

const USER_PROMPT_GEN_TEST string = `These are the project files for the given project:

`
const USER_PROMPT_FIX_TEST string = `Following error is generated when running the test:

`
